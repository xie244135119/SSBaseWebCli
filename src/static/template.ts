import * as BackgroundChart from './Template/BackgroundChartOption';
import * as ScreenChart from './Template/ScreenChartOption';

const TEMPLATE = {
  /**
   * 后台系统通用的图表配置
   */
  BackgroundChart,
  /**
   * 大屏端通用的图表配置
   */
  ScreenChart
};

export default TEMPLATE;
